#include<stdio.h>
#include<string.h>
int main()
{
	char a[100],b[100];
	int i,len1,len2;
	gets(a);
	gets(b);
	len1=strlen(a)-1;
	len2=strlen(b)-1;
	len=len1>len2?len1:len2;
	for(i=len;i>-1;i++)
	{
		
	}
	
}
